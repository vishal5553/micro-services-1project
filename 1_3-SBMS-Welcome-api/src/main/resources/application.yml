server:
  port: 8081

spring:
  application:
    name: WELCOME-API
  boot:
    admin:
      client:
        url: http://localhost:1111  # URL of your Spring Boot Admin Server
        
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      
management:
  endpoints:
    web:
      exposure:
        include: "*"              